# Visitor counter storage (Upstash Redis).
# On Vercel these are injected automatically once you connect the Upstash
# integration to the project — you only need them for local `vercel dev`.
#
# Copy to .env.local and fill in from the Upstash console (REST API section).
KV_REST_API_URL=
KV_REST_API_TOKEN=
